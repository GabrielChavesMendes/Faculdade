/**
  * Pontificia Universidade Catolica de Minas Gerais
  * Curso de Ciencia da Computacao
  * Algoritmos e Estruturas de Dados I
  *
  * Exercicio pratico: ed10_+_EXTRAS
  *
  * Matricula: 1453522  Nome: Gabriel_Chaves_Mendes
  * Versao:  1.0                Data: 07/05/2024
  *
  * Dados:
  * - Feito na IDE CodeBlocks
  *
  * Resultados(SAIDAS):
    * - Exercício 1011:
        Menu:
        1- Metodo 1011;
        2- Metodo 1012;
        3- Metodo 1013;
        4- Metodo 1014;
        5- Metodo 1015;
        6- Metodo 1016;
        7- Metodo 1017;
        8- Metodo 1018;
        9- Metodo 1019;
        10- Metodo 1020;
        11- Metodo 10E1_EXTRA1;
        12- Metodo 10E2_EXTRA2;
        Digite 0 para sair do programa
        Opcao: 1
        Executando o Metodo 1011.
        Escreva limite inferior: 1
        Escreva limite superior: 50
        Quantos numeros aleatorios serao alocados: 10
        No arquivo:
        10 numeros aleatorios entre o intervalo de [1:50]:
        [42]
        [18]
        [35]
        [1]
        [20]
        [25]
        [29]
        [9]
        [13]
        [15]

    * - Exercício 1012:
        Menu:
        1- Metodo 1011;
        2- Metodo 1012;
        3- Metodo 1013;
        4- Metodo 1014;
        5- Metodo 1015;
        6- Metodo 1016;
        7- Metodo 1017;
        8- Metodo 1018;
        9- Metodo 1019;
        10- Metodo 1020;
        11- Metodo 10E1_EXTRA1;
        12- Metodo 10E2_EXTRA2;
        Digite 0 para sair do programa
        Opcao: 2
        Executando o Metodo 1012.
        Qual valor deseja encontrar dentro da cadeia: 10
        O valor nao foi encontrado na cadeia
        Menu:
        1- Metodo 1011;
        2- Metodo 1012;
        3- Metodo 1013;
        4- Metodo 1014;
        5- Metodo 1015;
        6- Metodo 1016;
        7- Metodo 1017;
        8- Metodo 1018;
        9- Metodo 1019;
        10- Metodo 1020;
        11- Metodo 10E1_EXTRA1;
        12- Metodo 10E2_EXTRA2;
        Digite 0 para sair do programa
        Opcao: 2
        Executando o Metodo 1012.
        Qual valor deseja encontrar dentro da cadeia: 18
        O valor 18 foi encontrado na cadeia

    * - Exercício 1013:
        Menu:
        1- Metodo 1011;
        2- Metodo 1012;
        3- Metodo 1013;
        4- Metodo 1014;
        5- Metodo 1015;
        6- Metodo 1016;
        7- Metodo 1017;
        8- Metodo 1018;
        9- Metodo 1019;
        10- Metodo 1020;
        11- Metodo 10E1_EXTRA1;
        12- Metodo 10E2_EXTRA2;
        Digite 0 para sair do programa
        Opcao: 3
        Executando o Metodo 1013.
        Escreva limite inferior: 1
        Escreva limite superior: 100
        Quantos numeros aleatorios serao alocados: 10
        O tamanho do primeiro arquivo e do segundo sao iguais
        [42] e diferente de [6]
        [68] e diferente de [46]
        [35] e diferente de [82]
        [1] e diferente de [28]
        [70] e diferente de [62]
        [25] e diferente de [92]
        [79] e diferente de [96]
        [59] e diferente de [43]
        [63] e diferente de [28]
        [65] e diferente de [37]
        Os arquivo nao sao iguais

    * - Exercício 1014:
        Menu:
        1- Metodo 1011;
        2- Metodo 1012;
        3- Metodo 1013;
        4- Metodo 1014;
        5- Metodo 1015;
        6- Metodo 1016;
        7- Metodo 1017;
        8- Metodo 1018;
        9- Metodo 1019;
        10- Metodo 1020;
        11- Metodo 10E1_EXTRA1;
        12- Metodo 10E2_EXTRA2;
        Digite 0 para sair do programa
        Opcao: 4
        Executando o Metodo 1014.
        A soma dos arquivos e:
        [48]
        [114]
        [117]
        [29]
        [132]
        [117]
        [175]
        [102]
        [91]
        [102]

    * - Exercício 1015:
        Menu:
        1- Metodo 1011;
        2- Metodo 1012;
        3- Metodo 1013;
        4- Metodo 1014;
        5- Metodo 1015;
        6- Metodo 1016;
        7- Metodo 1017;
        8- Metodo 1018;
        9- Metodo 1019;
        10- Metodo 1020;
        11- Metodo 10E1_EXTRA1;
        12- Metodo 10E2_EXTRA2;
        Digite 0 para sair do programa
        Opcao: 5
        Executando o Metodo 1015.
        Quandos numeros serao escritos no arquivo: 5
        Escreva os numeros:
        5
        4
        3
        2
        1
        O vetor ta em ordem decrescente

    * - Exercício 1016:
        Menu:
        1- Metodo 1011;
        2- Metodo 1012;
        3- Metodo 1013;
        4- Metodo 1014;
        5- Metodo 1015;
        6- Metodo 1016;
        7- Metodo 1017;
        8- Metodo 1018;
        9- Metodo 1019;
        10- Metodo 1020;
        11- Metodo 10E1_EXTRA1;
        12- Metodo 10E2_EXTRA2;
        Digite 0 para sair do programa
        Opcao: 6
        Executando o Metodo 1016.
        Escreva quantas linhas tera a matriz: 3
        Escreva quantas colunas tera a matriz: 2
        Escreva um numero para a posicao [0][0]: 1
        Escreva um numero para a posicao [0][1]: 2
        Escreva um numero para a posicao [1][0]: 3
        Escreva um numero para a posicao [1][1]: 4
        Escreva um numero para a posicao [2][0]: 5
        Escreva um numero para a posicao [2][1]: 6
        Matriz do arquivo:
        [1]     [2]
        [3]     [4]
        [5]     [6]
        Matriz transposta:
        [1]     [3]     [5]
        [2]     [4]     [6]

    * - Exercício 1017:
        Menu:
        1- Metodo 1011;
        2- Metodo 1012;
        3- Metodo 1013;
        4- Metodo 1014;
        5- Metodo 1015;
        6- Metodo 1016;
        7- Metodo 1017;
        8- Metodo 1018;
        9- Metodo 1019;
        10- Metodo 1020;
        11- Metodo 10E1_EXTRA1;
        12- Metodo 10E2_EXTRA2;
        Digite 0 para sair do programa
        Opcao: 7
        Executando o Metodo 1017.
        Matriz do arquivo:
        Os valores dentro da matriz nao sao todos 0
        Matriz analisada:
        [1]     [2]
        [3]     [4]
        [5]     [6]

    * - Exercício 1018:
        Menu:
        1- Metodo 1011;
        2- Metodo 1012;
        3- Metodo 1013;
        4- Metodo 1014;
        5- Metodo 1015;
        6- Metodo 1016;
        7- Metodo 1017;
        8- Metodo 1018;
        9- Metodo 1019;
        10- Metodo 1020;
        11- Metodo 10E1_EXTRA1;
        12- Metodo 10E2_EXTRA2;
        Digite 0 para sair do programa
        Opcao: 8
        Executando o Metodo 1018.
        Escreva quantas linhas tera a matriz: 2
        Escreva quantas colunas tera a matriz: 2
        Primeira matriz
        Escreva um numero para a posicao [0][0]: 1
        Escreva um numero para a posicao [0][1]: 2
        Escreva um numero para a posicao [1][0]: 3
        Escreva um numero para a posicao [1][1]: 4
        Escreva quantas linhas tera a matriz2: 2
        Escreva quantas colunas tera a matriz2: 2
        Segunda matriz
        Escreva um numero para a posicao [0][0]: 4
        Escreva um numero para a posicao [0][1]: 3
        Escreva um numero para a posicao [1][0]: 2
        Escreva um numero para a posicao [1][1]: 1
        Matriz do primeiro arquivo:
        [1]     [2]
        [3]     [4]
        Matriz do segundo arquivo:
        [4]     [3]
        [2]     [1]
        As matrizes tem a mesma dimensao
        Mas matrizes nao sao iguais
        Menu:
        1- Metodo 1011;
        2- Metodo 1012;
        3- Metodo 1013;
        4- Metodo 1014;
        5- Metodo 1015;
        6- Metodo 1016;
        7- Metodo 1017;
        8- Metodo 1018;
        9- Metodo 1019;
        10- Metodo 1020;
        11- Metodo 10E1_EXTRA1;
        12- Metodo 10E2_EXTRA2;
        Digite 0 para sair do programa
        Opcao: 8
        Executando o Metodo 1018.
        Escreva quantas linhas tera a matriz: 2
        Escreva quantas colunas tera a matriz: 2
        Primeira matriz
        Escreva um numero para a posicao [0][0]: 1
        Escreva um numero para a posicao [0][1]: 2
        Escreva um numero para a posicao [1][0]: 3
        Escreva um numero para a posicao [1][1]: 4
        Escreva quantas linhas tera a matriz2: 2
        Escreva quantas colunas tera a matriz2: 2
        Segunda matriz
        Escreva um numero para a posicao [0][0]: 1
        Escreva um numero para a posicao [0][1]: 2
        Escreva um numero para a posicao [1][0]: 3
        Escreva um numero para a posicao [1][1]: 4
        Matriz do primeiro arquivo:
        [1]     [2]
        [3]     [4]
        Matriz do segundo arquivo:
        [1]     [2]
        [3]     [4]
        As matrizes tem a mesma dimensao
        E as matrizes sao iguais

    * - Exercício 1019:
        Menu:
        1- Metodo 1011;
        2- Metodo 1012;
        3- Metodo 1013;
        4- Metodo 1014;
        5- Metodo 1015;
        6- Metodo 1016;
        7- Metodo 1017;
        8- Metodo 1018;
        9- Metodo 1019;
        10- Metodo 1020;
        11- Metodo 10E1_EXTRA1;
        12- Metodo 10E2_EXTRA2;
        Digite 0 para sair do programa
        Opcao: 9
        Executando o Metodo 1019.
        Matriz do primeiro arquivo:
        [1]     [2]
        [3]     [4]
        Matriz do segundo arquivo:
        [1]     [2]
        [3]     [4]
        As matrizes tem a mesma dimensao
        A matriz resultante da soma das outras duas e:
        [2]     [4]
        [6]     [8]

    * - Exercício 1020:
        Menu:
        1- Metodo 1011;
        2- Metodo 1012;
        3- Metodo 1013;
        4- Metodo 1014;
        5- Metodo 1015;
        6- Metodo 1016;
        7- Metodo 1017;
        8- Metodo 1018;
        9- Metodo 1019;
        10- Metodo 1020;
        11- Metodo 10E1_EXTRA1;
        12- Metodo 10E2_EXTRA2;
        Digite 0 para sair do programa
        Opcao: 10
        Executando o Metodo 1020.
        Matriz do primeiro arquivo:
        [1]     [2]
        [3]     [4]
        Matriz do segundo arquivo:
        [1]     [2]
        [3]     [4]
        As matrizes tem a mesma dimensao
        A matriz resultante do produto das outras duas e:
        [1]     [4]
        [9]     [16]

    * - Exercício 10E1:
        Menu:
        1- Metodo 1011;
        2- Metodo 1012;
        3- Metodo 1013;
        4- Metodo 1014;
        5- Metodo 1015;
        6- Metodo 1016;
        7- Metodo 1017;
        8- Metodo 1018;
        9- Metodo 1019;
        10- Metodo 1020;
        11- Metodo 10E1_EXTRA1;
        12- Metodo 10E2_EXTRA2;
        Digite 0 para sair do programa
        Opcao: 11
        Executando o Metodo 10E1.
        Escreva quantas linhas tera a matriz: 2
        Escreva quantas colunas tera a matriz: 2
        Preencha a matriz em ordem crescente:
        Escreva um numero para a posicao [0][0]: 1
        Escreva um numero para a posicao [0][1]: 2
        Escreva um numero para a posicao [1][0]: 3
        Escreva um numero para a posicao [1][1]: 4
        Matriz do arquivo:
        [1]     [2]
        [3]     [4]
        Matriz decrescente:
        [4]     [3]
        [2]     [1]

    * - Exercício 10E2:
        Menu:
        1- Metodo 1011;
        2- Metodo 1012;
        3- Metodo 1013;
        4- Metodo 1014;
        5- Metodo 1015;
        6- Metodo 1016;
        7- Metodo 1017;
        8- Metodo 1018;
        9- Metodo 1019;
        10- Metodo 1020;
        11- Metodo 10E1_EXTRA1;
        12- Metodo 10E2_EXTRA2;
        Digite 0 para sair do programa
        Opcao: 12
        Executando o Metodo 10E2.
        Matriz do primeiro arquivo:
        [1]     [2]
        [3]     [4]
        Matriz do segundo arquivo:
        [1]     [2]
        [3]     [4]
        As matrizes tem a mesma dimensao
        A matriz resultante do produto das outras duas e:
        [1]     [4]
        [9]     [16]
        Matriz identidade:
        [1]     [0]
        [0]     [1]
        A matriz produto e a matriz identidade nao sao iguais
    */



//BIBLIOTECAS
#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <stdbool.h>

//--------METODOS AUXILIARES--------//

typedef struct{
    int minimo;
    int maximo;
    int N;
    int cont;
    int indice;
    int soma;
    int valor;

    char linha[100];

    bool achou;
}Numeros;

//------------------METODOS-----------------------//

int Minimo(Numeros *quantidade){

    printf("Escreva limite inferior: ");
    scanf("%d", &(quantidade->minimo));
}

int Maximo(Numeros *quantidade){

    printf("Escreva limite superior: ");
    scanf("%d", &(quantidade->maximo));
}

int Numeros_Aleatorios(Numeros *quantidade){

    return quantidade->minimo + rand() % (quantidade->maximo - quantidade->minimo + 1);
}

void procedimento1011(void){

    Numeros numeros;

    Minimo(&numeros);
    Maximo(&numeros);

    printf("Quantos numeros aleatorios serao alocados: ");
    scanf("%d", &(numeros.N));

    int vetor[numeros.N];

    for(int i = 0; i < numeros.N; i++){
        vetor[i] = Numeros_Aleatorios(&numeros);
    }

    FILE *arquivo;
    arquivo = fopen("DADOS.TXT", "w");

    fprintf(arquivo, "%d numeros aleatorios entre o intervalo de [%d:%d]:\n", numeros.N, numeros.minimo, numeros.maximo);

    for(int i = 0; i < numeros.N; i++){
        fprintf(arquivo, "[%d]\n", vetor[i]);
    }

    fclose(arquivo);
}

//--------------------------------------------------//

int Encontrar_Numero(Numeros *achar){

    FILE *arquivo;
    arquivo = fopen("DADOS.TXT", "r");

    achar->cont = 0;

    fgets(achar->linha, sizeof(achar->linha), arquivo);

    while(fgets(achar->linha, sizeof(achar->linha), arquivo) != NULL){
        achar->cont++;
    }

    rewind(arquivo);

    fgets(achar->linha, sizeof(achar->linha), arquivo);

    int *vetor = (int *)malloc(achar->cont * sizeof(int));

    achar->indice = 0;
    achar->achou = false;

    for(int i = 0; i < achar->cont; i++){
        fscanf(arquivo, "[%d]\n", &vetor[i]);
    }

    printf("\nQual valor deseja encontrar dentro da cadeia: ");
    scanf("%d", &achar->N);

    while(achar->indice < achar->cont && vetor[achar->indice] != achar->N){
        achar->indice++;
    }

    if(achar->achou = achar->indice < achar->cont){
        printf("O valor %d foi encontrado na cadeia", achar->N);
    }else{
        printf("O valor nao foi encontrado na cadeia");
    }

    free(vetor);
    fclose(arquivo);
}

void procedimento1012(void){

Numeros achar;

Encontrar_Numero(&achar);
}

//--------------------------------------------------//

int Minimo2(Numeros *quantidade){

    printf("Escreva limite inferior: ");
    scanf("%d", &(quantidade->minimo));
}

int Maximo2(Numeros *quantidade){

    printf("Escreva limite superior: ");
    scanf("%d", &(quantidade->maximo));
}

int Numeros_Aleatorios2(Numeros *quantidade){

    return quantidade->minimo + rand() % (quantidade->maximo - quantidade->minimo + 1);
}

void Comparar(const char *filename1, const char *filename2, Numeros *achar){

    Numeros numeros;
    achar->achou = false;
    int tamanho, cont1 = 0, cont2 = 0, cont3 = 0;

    FILE *arquivo1 = fopen(filename1, "r");
    FILE *arquivo2 = fopen(filename2, "r");

    fgets(achar->linha, sizeof(achar->linha), arquivo1);

    while (fgets(achar->linha, sizeof(achar->linha), arquivo1) != NULL){
        cont1++;
    }

    fgets(achar->linha, sizeof(achar->linha), arquivo2);

    while (fgets(achar->linha, sizeof(achar->linha), arquivo2) != NULL){
        cont2++;
    }

    rewind(arquivo1);
    rewind(arquivo2);
    int *vetor1 = (int *)malloc(cont1 * sizeof(int));
    int *vetor2 = (int *)malloc(cont2 * sizeof(int));

    fgets(achar->linha, sizeof(achar->linha), arquivo1);
    fgets(achar->linha, sizeof(achar->linha), arquivo2);

    for(int i = 0; i < cont1; i++){
        fscanf(arquivo1, "[%d]\n", &vetor1[i]);
    }

    for(int i = 0; i < cont2; i++){
        fscanf(arquivo2, "[%d]\n", &vetor2[i]);
    }

    if(cont1 == cont2){

        printf("O tamanho do primeiro arquivo e do segundo sao iguais\n");

        for(int i = 0; i < cont1; i++){
            for(int j = i; j <= i; j++){
                if(vetor1[i] == vetor2[j]){
                    printf("[%d] e igual a [%d]\n", vetor1[i], vetor2[j]);
                    cont3++;
                }else{
                    printf("[%d] e diferente de [%d]\n", vetor1[i], vetor2[j]);
                }
            }
        }

        if(cont3 == cont1){
            printf("\nOs arquivos sao iguais");
        }else{
            printf("\nOs arquivo nao sao iguais");
        }
    }else{
        printf("Os arquivos nao tem o mesmo tamanho");
    }

    free(vetor1);
    free(vetor2);

    fclose(arquivo1);
    fclose(arquivo2);

}

void procedimento1013(void){

    Numeros numeros;

    Minimo2(&numeros);
    Maximo2(&numeros);

    printf("Quantos numeros aleatorios serao alocados: ");
    scanf("%d", &(numeros.N));

    int vetor1[numeros.N];
    int vetor2[numeros.N];

    for(int i = 0; i < numeros.N; i++){
        vetor1[i] = Numeros_Aleatorios2(&numeros);
    }

    for(int i = 0; i < numeros.N; i++){
        vetor2[i] = Numeros_Aleatorios2(&numeros);
    }


    FILE *arquivo1;
    FILE *arquivo2;
    arquivo1 = fopen("DADOS1.TXT", "w");
    arquivo2 = fopen("DADOS2.TXT", "w");

    fprintf(arquivo1, "%d numeros aleatorios entre o intervalo de [%d:%d]:\n", numeros.N, numeros.minimo, numeros.maximo);
    fprintf(arquivo2, "%d numeros aleatorios entre o intervalo de [%d:%d]:\n", numeros.N, numeros.minimo, numeros.maximo);

    for(int i = 0; i < numeros.N; i++){
        fprintf(arquivo1, "[%d]\n", vetor1[i]);
    }

    for(int i = 0; i < numeros.N; i++){
        fprintf(arquivo2, "[%d]\n", vetor2[i]);
    }

    fclose(arquivo1);
    fclose(arquivo2);

    Comparar("DADOS1.TXT", "DADOS2.TXT", &numeros);
}

//--------------------------------------------------//

void Soma_Arquivos(const char *name1, const char name2, Numeros *somando){

    FILE *arquivo1 = fopen("DADOS1.TXT", "r");
    FILE *arquivo2 = fopen("DADOS2.TXT", "r");

    int cont1 = 0, cont2 = 0;

    fgets(somando->linha, sizeof(somando->linha), arquivo1);

    while(fgets(somando->linha, sizeof(somando->linha), arquivo1) != NULL){
        cont1++;
    }

    fgets(somando->linha, sizeof(somando->linha), arquivo2);

    while(fgets(somando->linha, sizeof(somando->linha), arquivo2) != NULL){
        cont2++;
    }

    rewind(arquivo1);
    rewind(arquivo2);

    int *vetor1 = (int *)malloc(cont1 * sizeof(int));
    int *vetor2 = (int *)malloc(cont2 * sizeof(int));

    fgets(somando->linha, sizeof(somando->linha), arquivo1);

    for(int i = 0; i < cont1; i++){
        fscanf(arquivo1, "[%d]\n", &vetor1[i]);
    }

    fgets(somando->linha, sizeof(somando->linha), arquivo2);

    for(int i = 0; i < cont2; i++){
        fscanf(arquivo2, "[%d]\n", &vetor2[i]);
    }

    somando->soma = 0;

    if(cont1 == cont2){
        int *vetor3 = (int *)malloc(cont1 * sizeof(int));

        printf("A soma dos arquivos e:\n");

        for(int i = 0; i < cont1; i++){
            for(int j = i; j <= i; j++){
               somando->soma = vetor1[i] + vetor2[j];
               vetor3[i] = somando->soma;
               printf("[%d]\n", vetor3[i]);
            }
        }
    }else{
        printf("Os arquivos nao sao de mesmo tamanho");
    }
}


void procedimento1014(void){

    Numeros numeros;

    Soma_Arquivos("DADOS1.TXT", "DADOS2.TXT", &numeros);
}

//--------------------------------------------------//

bool Ordem_Decrescente(const char *name, Numeros *ordem){

    FILE *arquivo = fopen(name, "r");

    ordem->cont = 0;
    ordem->indice = 0;
    ordem->achou = false;
    int controle = 1, cont2 = 0;

    while(fgets(ordem->linha, sizeof(ordem->linha), arquivo) != NULL){
        ordem->cont++;
    }

    rewind(arquivo);

    int *vetor = (int *)malloc(ordem->cont * sizeof(int));

    for(int i = 0; i < ordem->cont; i++){
        fscanf(arquivo, "[%d]\n", &vetor[i]);
    }

    for(int i = 0; i < ordem->cont - 1; i++){
        for(int j = i + 1; j < ordem->cont; j++){
            if(vetor[i] > vetor[j]){
                ordem->indice++;
            }
        }
    }

    fclose(arquivo);

    if(ordem->cont * 2 == ordem->indice){
        return  ordem->achou = true;
    }else{
        return ordem->achou;
    }
}

void procedimento1015(void){

    Numeros numeros;

    FILE *arquivo = fopen("DADOS3.TXT", "w");

    printf("Quandos numeros serao escritos no arquivo: ");
    scanf("%d", &numeros.N);

    printf("Escreva os numeros:\n");
    for(int i = 0; i < numeros.N; i++){
        scanf("%d", &numeros.valor);
        fprintf(arquivo, "[%d]\n", numeros.valor);
    }

    fclose(arquivo);

    numeros.achou = Ordem_Decrescente("DADOS3.TXT", &numeros);

    if(numeros.achou == true){
        printf("O vetor ta em ordem decrescente");
    }else{
        printf("O vetor nao ta em ordem decrescente");
    }
}

//--------------------------------------------------//

void Matriz_Transposta(const char *name, Numeros *matriz){

    FILE *arquivo = fopen(name, "r");
    int colunas = 0, linhas = 0, c;

    //LER QUANTAS LINHAS
    while(fgets(matriz->linha, sizeof(matriz->linha), arquivo) != NULL){
        linhas++;
    }

    rewind(arquivo);

    //LER QUANTAS COLUNAS
    while((c = fgetc(arquivo)) != EOF){ //FGETC RETORNAR INTEIRO
        if(c == ' ' || c == '\t'){
            colunas++;
        }
        if(c == '\n'){
        }
    }

    colunas = colunas / linhas;

    int matriz1[linhas][colunas];

    rewind(arquivo);

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            fscanf(arquivo, "[%d]\t", &matriz1[i][j]);
        }
        fscanf(arquivo, "\n");
    }

    printf("Matriz do arquivo:\n");

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            printf("[%d]\t", matriz1[i][j]);
        }
        printf("\n");
    }

    fclose(arquivo);

    int matriz2[colunas][linhas];

    printf("\nMatriz transposta:\n");

    for(int i = 0; i < colunas; i++){
        for(int j = 0; j < linhas; j++){
            matriz2[i][j] = matriz1[j][i];
            printf("[%d]\t", matriz2[i][j]);
        }
        printf("\n");
    }
}

void procedimento1016(void){

    Numeros numeros;
    FILE *arquivo = fopen("DADOS4.TXT", "w");
    int linhas, colunas;

    do{
        printf("Escreva quantas linhas tera a matriz: ");
        scanf("%d", &linhas);

        printf("Escreva quantas colunas tera a matriz: ");
        scanf("%d", &colunas);

        if(linhas == colunas){
            printf("A matriz nao pode ser quadrada!\n");
        }

    }while(linhas == colunas);

    int matriz[linhas][colunas];

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            printf("Escreva um numero para a posicao [%d][%d]: ", i, j);
            scanf("%d", &matriz[i][j]);
            fprintf(arquivo, "[%d]\t", matriz[i][j]);
        }
        fprintf(arquivo, "\n");
    }
    fclose(arquivo);

    Matriz_Transposta("DADOS4.TXT", &numeros);
}

//--------------------------------------------------//

void Apenas_0(const char *name, Numeros *zero){

    FILE *arquivo = fopen(name, "r");
    int colunas = 0, linhas = 0, c;

    //LER QUANTAS LINHAS
    while(fgets(zero->linha, sizeof(zero->linha), arquivo) != NULL){
        linhas++;
    }

    rewind(arquivo);

    //LER QUANTAS COLUNAS
    while((c = fgetc(arquivo)) != EOF){ //FGETC RETORNAR INTEIRO
        if(c == ' ' || c == '\t'){
            colunas++;
        }
        if(c == '\n'){
        }
    }

    colunas = colunas / linhas;

    int matriz1[linhas][colunas];
    zero->cont = 0;
    zero->indice = 0;

    rewind(arquivo);

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            fscanf(arquivo, "[%d]\t", &matriz1[i][j]);
        }
        fscanf(arquivo, "\n");
    }

    printf("Matriz do arquivo:\n");

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            if(matriz1[i][j] == 0){
                zero->cont++;
            }
            zero->indice++;
        }
    }

    fclose(arquivo);

    if(zero->cont == zero->indice){
        printf("Os valores dentro da matriz sao todos 0");
    }else{
        printf("Os valores dentro da matriz nao sao todos 0");
    }

    printf("\nMatriz analisada:\n");

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
           printf("[%d]\t", matriz1[i][j]);
        }
        printf("\n");
    }
}

void procedimento1017(void){

    Numeros numeros;
    Apenas_0("DADOS4.TXT", &numeros);
}

//--------------------------------------------------//

void Compara_Matriz(const char *name, const char *name2, Numeros *compara){

    FILE *arquivo = fopen(name, "r");
    FILE *arquivo2 = fopen(name2, "r");
    int colunas = 0, linhas = 0, c;
    int colunas2 = 0, linhas2 = 0;
    char linha[100];

    //LER QUANTAS LINHAS
    while(fgets(compara->linha, sizeof(compara->linha), arquivo) != NULL){
        linhas++;
    }

    while(fgets(linha, sizeof(linha), arquivo2) != NULL){
        linhas2++;
    }

    rewind(arquivo);
    rewind(arquivo2);

    //LER QUANTAS COLUNAS
    while((c = fgetc(arquivo)) != EOF){ //FGETC RETORNAR INTEIRO
        if(c == ' ' || c == '\t'){
            colunas++;
        }
        if(c == '\n'){
        }
    }

    while((c = fgetc(arquivo2)) != EOF){ //FGETC RETORNAR INTEIRO
        if(c == ' ' || c == '\t'){
            colunas2++;
        }
        if(c == '\n'){
        }
    }

    colunas = colunas / linhas;
    colunas2 = colunas2 / linhas2;

    int matriz1[linhas][colunas];
    int matriz2[linhas2][colunas2];
    compara->indice = 0;
    compara->cont = 0;

    rewind(arquivo);
    rewind(arquivo2);

    //PRIMEIRO ARQUIVO
    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            fscanf(arquivo, "[%d]\t", &matriz1[i][j]);
        }
        fscanf(arquivo, "\n");
    }

    printf("Matriz do primeiro arquivo:\n");

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
           printf("[%d]\t", matriz1[i][j]);
        }
        printf("\n");
    }

    fclose(arquivo);

    //SEGUNDO ARQUIVO
    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            fscanf(arquivo2, "[%d]\t", &matriz2[i][j]);
        }
        fscanf(arquivo2, "\n");
    }

    printf("Matriz do segundo arquivo:\n");

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
           printf("[%d]\t", matriz2[i][j]);
        }
        printf("\n");
    }

    fclose(arquivo2);

    if(linhas == linhas2 && colunas == colunas2){

        printf("As matrizes tem a mesma dimensao\n");

        for(int i = 0; i < linhas; i++){
            for(int j = 0; j < colunas; j++){
                if(matriz1[i][j] == matriz2[i][j]){
                    compara->cont++;
                }
                compara->indice++;
            }
        }

        if(compara->cont == compara->indice){
            printf("E as matrizes sao iguais");
        }else{
            printf("Mas matrizes nao sao iguais");
        }

    }else{
        printf("As matrizes nao tem a mesmas dimensoes");
    }
}


void procedimento1018(void){

    Numeros numeros;
    FILE *arquivo = fopen("DADOS5.TXT", "w");
    FILE *arquivo2 = fopen("DADOS6.TXT", "w");
    int linhas, colunas;

    printf("Escreva quantas linhas tera a matriz: ");
    scanf("%d", &linhas);

    printf("Escreva quantas colunas tera a matriz: ");
    scanf("%d", &colunas);

    int matriz[linhas][colunas];

    printf("Primeira matriz\n");
    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            printf("Escreva um numero para a posicao [%d][%d]: ", i, j);
            scanf("%d", &matriz[i][j]);
            fprintf(arquivo, "[%d]\t", matriz[i][j]);
        }
        fprintf(arquivo, "\n");
    }

    printf("Escreva quantas linhas tera a matriz2: ");
    scanf("%d", &linhas);

    printf("Escreva quantas colunas tera a matriz2: ");
    scanf("%d", &colunas);

    int matriz2[linhas][colunas];

    printf("Segunda matriz\n");
    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            printf("Escreva um numero para a posicao [%d][%d]: ", i, j);
            scanf("%d", &matriz2[i][j]);
            fprintf(arquivo2, "[%d]\t", matriz2[i][j]);
        }
        fprintf(arquivo2, "\n");
    }

    fclose(arquivo);
    fclose(arquivo2);

    Compara_Matriz("DADOS5.TXT", "DADOS6.TXT", &numeros);
}

//--------------------------------------------------//

void Soma_Matrizes(const char *name, const char *name2, Numeros *compara){

    FILE *arquivo = fopen(name, "r");
    FILE *arquivo2 = fopen(name2, "r");
    int colunas = 0, linhas = 0, c;
    int colunas2 = 0, linhas2 = 0;
    char linha[100];

    //LER QUANTAS LINHAS
    while(fgets(compara->linha, sizeof(compara->linha), arquivo) != NULL){
        linhas++;
    }

    while(fgets(linha, sizeof(linha), arquivo2) != NULL){
        linhas2++;
    }

    rewind(arquivo);
    rewind(arquivo2);

    //LER QUANTAS COLUNAS
    while((c = fgetc(arquivo)) != EOF){ //FGETC RETORNAR INTEIRO
        if(c == ' ' || c == '\t'){
            colunas++;
        }
        if(c == '\n'){
        }
    }

    while((c = fgetc(arquivo2)) != EOF){ //FGETC RETORNAR INTEIRO
        if(c == ' ' || c == '\t'){
            colunas2++;
        }
        if(c == '\n'){
        }
    }

    colunas = colunas / linhas;
    colunas2 = colunas2 / linhas2;

    int matriz1[linhas][colunas];
    int matriz2[linhas2][colunas2];
    compara->indice = 0;
    compara->cont = 0;

    rewind(arquivo);
    rewind(arquivo2);

    //PRIMEIRO ARQUIVO
    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            fscanf(arquivo, "[%d]\t", &matriz1[i][j]);
        }
        fscanf(arquivo, "\n");
    }

    printf("Matriz do primeiro arquivo:\n");

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
           printf("[%d]\t", matriz1[i][j]);
        }
        printf("\n");
    }

    fclose(arquivo);

    //SEGUNDO ARQUIVO
    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            fscanf(arquivo2, "[%d]\t", &matriz2[i][j]);
        }
        fscanf(arquivo2, "\n");
    }

    printf("Matriz do segundo arquivo:\n");

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
           printf("[%d]\t", matriz2[i][j]);
        }
        printf("\n");
    }

    fclose(arquivo2);

    if(linhas == linhas2 && colunas == colunas2){

        int matriz3[linhas][colunas];

        printf("As matrizes tem a mesma dimensao\n");
        printf("A matriz resultante da soma das outras duas e:\n");

        for(int i = 0; i < linhas; i++){
            for(int j = 0; j < colunas; j++){
                matriz3[i][j] = matriz1[i][j] + matriz2[i][j];
                printf("[%d]\t", matriz3[i][j]);
            }
            printf("\n");
        }
    }else{
        printf("As matrizes nao tem a mesmas dimensoes");
    }
}

void procedimento1019(void){

    Numeros numeros;

    Soma_Matrizes("DADOS5.TXT", "DADOS6.TXT", &numeros);
}

//--------------------------------------------------//

void Produto_Matrizes(const char *name, const char *name2, Numeros *compara){

    FILE *arquivo = fopen(name, "r");
    FILE *arquivo2 = fopen(name2, "r");
    int colunas = 0, linhas = 0, c;
    int colunas2 = 0, linhas2 = 0;
    char linha[100];

    //LER QUANTAS LINHAS
    while(fgets(compara->linha, sizeof(compara->linha), arquivo) != NULL){
        linhas++;
    }

    while(fgets(linha, sizeof(linha), arquivo2) != NULL){
        linhas2++;
    }

    rewind(arquivo);
    rewind(arquivo2);

    //LER QUANTAS COLUNAS
    while((c = fgetc(arquivo)) != EOF){ //FGETC RETORNAR INTEIRO
        if(c == ' ' || c == '\t'){
            colunas++;
        }
        if(c == '\n'){
        }
    }

    while((c = fgetc(arquivo2)) != EOF){ //FGETC RETORNAR INTEIRO
        if(c == ' ' || c == '\t'){
            colunas2++;
        }
        if(c == '\n'){
        }
    }

    colunas = colunas / linhas;
    colunas2 = colunas2 / linhas2;

    int matriz1[linhas][colunas];
    int matriz2[linhas2][colunas2];
    compara->indice = 0;
    compara->cont = 0;

    rewind(arquivo);
    rewind(arquivo2);

    //PRIMEIRO ARQUIVO
    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            fscanf(arquivo, "[%d]\t", &matriz1[i][j]);
        }
        fscanf(arquivo, "\n");
    }

    printf("Matriz do primeiro arquivo:\n");

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
           printf("[%d]\t", matriz1[i][j]);
        }
        printf("\n");
    }

    fclose(arquivo);

    //SEGUNDO ARQUIVO
    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            fscanf(arquivo2, "[%d]\t", &matriz2[i][j]);
        }
        fscanf(arquivo2, "\n");
    }

    printf("Matriz do segundo arquivo:\n");

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
           printf("[%d]\t", matriz2[i][j]);
        }
        printf("\n");
    }

    fclose(arquivo2);

    if(linhas == linhas2 && colunas == colunas2){

        int matriz3[linhas][colunas];

        printf("As matrizes tem a mesma dimensao\n");
        printf("A matriz resultante do produto das outras duas e:\n");

        for(int i = 0; i < linhas; i++){
            for(int j = 0; j < colunas; j++){
                matriz3[i][j] = matriz1[i][j] * matriz2[i][j];
                printf("[%d]\t", matriz3[i][j]);
            }
            printf("\n");
        }
    }else{
        printf("As matrizes nao tem a mesmas dimensoes");
    }
}

void procedimento1020(void){

    Numeros numeros;

    Produto_Matrizes("DADOS5.TXT", "DADOS6.TXT", &numeros);
}

//--------------------------------------------------//

void Matriz_Decrescente(const char *name, Numeros *decresce){

    FILE *arquivo = fopen(name, "r");
    int colunas = 0, linhas = 0, c;
    char linha[100];

    //LER QUANTAS LINHAS
    while(fgets(decresce->linha, sizeof(decresce->linha), arquivo) != NULL){
        linhas++;
    }

    rewind(arquivo);

    //LER QUANTAS COLUNAS
    while((c = fgetc(arquivo)) != EOF){ //FGETC RETORNAR INTEIRO
        if(c == ' ' || c == '\t'){
            colunas++;
        }
        if(c == '\n'){
        }
    }

    colunas = colunas / linhas;

    int matriz1[linhas][colunas];
    decresce->cont = 1;

    rewind(arquivo);

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            fscanf(arquivo, "[%d]\t", &matriz1[i][j]);
        }
        fscanf(arquivo, "\n");
    }

    fclose(arquivo);

    printf("Matriz do arquivo:\n");

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
           printf("[%d]\t", matriz1[i][j]);
        }
        printf("\n");
    }

    for (int i = 0; i < linhas; i++) {
        for (int j = 0; j < colunas; j++) {
            for (int k = j + 1; k < colunas; k++) {
                if (matriz1[i][k] > matriz1[i][j]) {
                    int temp = matriz1[i][j];
                    matriz1[i][j] = matriz1[i][k];
                    matriz1[i][k] = temp;
                }
            }
        }
    }

    printf("\nMatriz decrescente:\n");
    for (int i = linhas - 1; i >= 0; i--) {
        for (int j = 0; j < colunas; j++) {
            printf("[%d]\t", matriz1[i][j]);
        }
        printf("\n");
    }

}


void procedimento10E1(void){

    Numeros numeros;
    FILE *arquivo = fopen("DADOS7.TXT", "w");
    int colunas, linhas;

    printf("Escreva quantas linhas tera a matriz: ");
    scanf("%d", &linhas);

    printf("Escreva quantas colunas tera a matriz: ");
    scanf("%d", &colunas);

    int matriz[linhas][colunas];

    printf("Preencha a matriz em ordem crescente:\n");

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            printf("Escreva um numero para a posicao [%d][%d]: ", i, j);
            scanf("%d", &matriz[i][j]);
            fprintf(arquivo, "[%d]\t", matriz[i][j]);
        }
        fprintf(arquivo, "\n");
    }

    fclose(arquivo);

    Matriz_Decrescente("DADOS7.TXT", &numeros);
}

//--------------------------------------------------//


void Matriz_Identidade(const char *name, const char *name2, Numeros *compara){

    FILE *arquivo = fopen(name, "r");
    FILE *arquivo2 = fopen(name2, "r");
    int colunas = 0, linhas = 0, c;
    int colunas2 = 0, linhas2 = 0;
    char linha[100];

    //LER QUANTAS LINHAS
    while(fgets(compara->linha, sizeof(compara->linha), arquivo) != NULL){
        linhas++;
    }

    while(fgets(linha, sizeof(linha), arquivo2) != NULL){
        linhas2++;
    }

    rewind(arquivo);
    rewind(arquivo2);

    //LER QUANTAS COLUNAS
    while((c = fgetc(arquivo)) != EOF){ //FGETC RETORNAR INTEIRO
        if(c == ' ' || c == '\t'){
            colunas++;
        }
        if(c == '\n'){
        }
    }

    while((c = fgetc(arquivo2)) != EOF){ //FGETC RETORNAR INTEIRO
        if(c == ' ' || c == '\t'){
            colunas2++;
        }
        if(c == '\n'){
        }
    }

    colunas = colunas / linhas;
    colunas2 = colunas2 / linhas2;

    int matriz1[linhas][colunas];
    int matriz2[linhas2][colunas2];
    compara->indice = 0;
    compara->cont = 0;

    rewind(arquivo);
    rewind(arquivo2);

    //PRIMEIRO ARQUIVO
    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            fscanf(arquivo, "[%d]\t", &matriz1[i][j]);
        }
        fscanf(arquivo, "\n");
    }

    printf("Matriz do primeiro arquivo:\n");

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
           printf("[%d]\t", matriz1[i][j]);
        }
        printf("\n");
    }

    fclose(arquivo);

    //SEGUNDO ARQUIVO
    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
            fscanf(arquivo2, "[%d]\t", &matriz2[i][j]);
        }
        fscanf(arquivo2, "\n");
    }

    printf("Matriz do segundo arquivo:\n");

    for(int i = 0; i < linhas; i++){
        for(int j = 0; j < colunas; j++){
           printf("[%d]\t", matriz2[i][j]);
        }
        printf("\n");
    }

    fclose(arquivo2);

    if(linhas == linhas2 && colunas == colunas2){

        int matriz3[linhas][colunas];

        printf("As matrizes tem a mesma dimensao\n");
        printf("A matriz resultante do produto das outras duas e:\n");

        for(int i = 0; i < linhas; i++){
            for(int j = 0; j < colunas; j++){
                matriz3[i][j] = matriz1[i][j] * matriz2[i][j];
                printf("[%d]\t", matriz3[i][j]);
            }
            printf("\n");
        }

        int matriz4[linhas][colunas];

        printf("Matriz identidade:\n");
        for(int i = 0; i < linhas; i++){
            for(int j = 0; j < colunas; j++){
                if(i == j){
                    matriz4[i][j] = 1;
                }else{
                    matriz4[i][j] = 0;
                }
                printf("[%d]\t", matriz4[i][j]);
            }
            printf("\n");
        }

        for(int i = 0; i < linhas; i++){
            for(int j = 0; j < colunas; j++){
                if(matriz3[i][j] == matriz4[i][j]){
                    compara->cont++;
                }
                    compara->indice++;
            }
            printf("\n");
        }

        if(compara->cont == compara->indice){
            printf("A matriz produto e a matriz identidade sao iguais");
        }else{
            printf("A matriz produto e a matriz identidade nao sao iguais");
        }

    }else{
        printf("As matrizes nao tem a mesmas dimensoes");
    }
}

void procedimento10E2(void){

    Numeros numeros;

    Matriz_Identidade("DADOS5.TXT", "DADOS6.TXT", &numeros);
}

//--------------------------------------------------//

int main()
{
    int opcao;
    char linha[60];

   do{
    printf("Menu:\n");
     printf("1- Metodo 1011;\n"
            "2- Metodo 1012;\n"
            "3- Metodo 1013;\n"
            "4- Metodo 1014;\n"
            "5- Metodo 1015;\n"
            "6- Metodo 1016;\n"
            "7- Metodo 1017;\n"
            "8- Metodo 1018;\n"
            "9- Metodo 1019;\n"
            "10- Metodo 1020;\n"
            "11- Metodo 10E1_EXTRA1;\n"
            "12- Metodo 10E2_EXTRA2;\n"
            "Digite 0 para sair do programa\n\n"
            "Opcao: ");
     scanf("%d", &opcao);

    switch(opcao){
        case 0:
            break;

        case 1:
            printf("Executando o Metodo 1011.\n");
            procedimento1011();
            break;

        case 2:
            printf("Executando o Metodo 1012.\n");
            procedimento1012();
            break;

        case 3:
            printf("Executando o Metodo 1013.\n");
            procedimento1013();
            break;

        case 4:
            printf("Executando o Metodo 1014.\n");
            procedimento1014();
            break;

        case 5:
            printf("Executando o Metodo 1015.\n");
            procedimento1015();
            break;

        case 6:
            printf("Executando o Metodo 1016.\n");
            procedimento1016();
            break;

        case 7:
            printf("Executando o Metodo 1017.\n");
            procedimento1017();
            break;

        case 8:
            printf("Executando o Metodo 1018.\n");
            procedimento1018();
            break;

        case 9:
            printf("Executando o Metodo 1019.\n");
            procedimento1019();
            break;

        case 10:
            printf("Executando o Metodo 1020.\n");
            procedimento1020();
            break;

        case 11:
            printf("Executando o Metodo 10E1.\n");
            procedimento10E1();
            break;

        case 12:
            printf("Executando o Metodo 10E2.\n");
            procedimento10E2();
            break;

        default:
            printf("Opcao invalida. Escolha uma opcao de 1 a 12.\n");
            break;
    }

      printf("\n");
    }while(opcao != 0);

return 0;
}
